# MusicMaster 快速启动指南

## 🚀 三种快速启动方式

### 方式一：使用 Docker（推荐，最简单）

#### 前置条件
- 已安装 Docker
- 已安装 Docker Compose

#### 启动步骤

1. **启动 MySQL 容器**
   ```bash
   cd MusicMaster
   docker-compose up -d mysql
   ```

2. **初始化数据库**
   ```bash
   docker exec -i musicmaster-mysql mysql -uroot -proot123 musicmaster < backend/src/main/resources/sql/init.sql
   ```

3. **启动后端服务**
   ```bash
   docker-compose up -d backend
   ```

4. **启动前端服务**
   ```bash
   cd frontend
   npm install
   npm run serve
   ```

5. **访问系统**
   - 打开浏览器访问：http://localhost:8080
   - 默认管理员账号：admin / admin123

---

### 方式二：使用本地环境（标准方式）

#### 前置条件检查

检查以下工具是否已安装：

```bash
# 检查 JDK
java -version

# 检查 MySQL
mysql --version

# 检查 Node.js
node -v
npm -v
```

#### 详细步骤

**1. 安装缺失的工具（如果需要）**

- **安装 JDK 11**：https://adoptium.net/
- **安装 MySQL 8**：https://dev.mysql.com/downloads/mysql/
- **安装 Node.js 18**：https://nodejs.org/

**2. 安装 Maven**

```bash
# 下载 Maven
wget https://downloads.apache.org/maven/maven-3/3.8.6/binaries/apache-maven-3.8.6-bin.tar.gz

# 解压
tar -xzf apache-maven-3.8.6-bin.tar.gz
sudo mv apache-maven-3.8.6 /opt/maven

# 配置环境变量
echo 'export PATH=/opt/maven/bin:$PATH' >> ~/.bashrc
source ~/.bashrc

# 验证
mvn -version
```

**3. 配置数据库**

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE musicmaster DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 导入数据
USE musicmaster;
SOURCE backend/src/main/resources/sql/init.sql;

# 退出
exit;
```

**4. 启动后端**

```bash
cd backend

# 修改配置文件中的数据库密码
# 编辑 backend/src/main/resources/application.yml

# 编译并运行
mvn clean install -DskipTests
mvn spring-boot:run
```

**5. 启动前端**

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run serve
```

**6. 访问系统**

打开浏览器访问：http://localhost:8080

---

### 方式三：使用 IDEA（开发者推荐）

#### 前置条件
- 已安装 IntelliJ IDEA（社区版或旗舰版）
- 已安装 MySQL

#### 启动步骤

**1. 导入后端项目**

1. 打开 IDEA，选择 "Open"
2. 选择 `MusicMaster/backend` 目录
3. 等待 IDEA 自动导入 Maven 依赖

**2. 配置数据库**

1. 打开 `backend/src/main/resources/application.yml`
2. 修改数据库连接信息：
   ```yaml
   spring:
     datasource:
       username: root
       password: 你的MySQL密码
   ```

**3. 运行后端**

1. 找到 `MusicMasterApplication.java`
2. 右键点击，选择 "Run 'MusicMasterApplication'"

**4. 运行前端**

```bash
cd frontend
npm install
npm run serve
```

**5. 访问系统**

打开浏览器访问：http://localhost:8080

---

## 📋 环境配置清单

### 必需环境

| 工具 | 版本要求 | 用途 | 是否必须 |
|------|---------|------|---------|
| JDK | 8+ (推荐 11) | 运行后端 | ✅ |
| MySQL | 8.0+ | 数据库 | ✅ |
| Node.js | 16+ | 运行前端 | ✅ |
| npm | 8+ | 包管理器 | ✅ |
| Maven | 3.6+ | 构建后端 | ⚠️ (Docker 方式不需要) |
| Docker | 20+ | 容器化部署 | ❌ (可选) |

### 推荐环境

| 工具 | 用途 | 是否推荐 |
|------|------|---------|
| IntelliJ IDEA | 后端开发 | ✅ |
| VS Code | 前端开发 | ✅ |
| Navicat | 数据库管理 | ✅ |
| Postman | API 测试 | ✅ |

---

## 🔧 常见问题快速解决

### 问题 1：Maven 下载依赖失败

**解决方案：**
```bash
# 使用阿里云镜像
# 编辑 ~/.m2/settings.xml，添加：
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

### 问题 2：npm install 慢或失败

**解决方案：**
```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
rm -rf node_modules package-lock.json
npm install
```

### 问题 3：数据库连接失败

**解决方案：**
1. 检查 MySQL 是否启动
   ```bash
   # Windows
   net start MySQL
   
   # Mac
   sudo /usr/local/mysql/support-files/mysql.server start
   
   # Linux
   sudo systemctl start mysql
   ```

2. 检查用户名和密码是否正确

3. 检查数据库是否已创建
   ```bash
   mysql -u root -p
   SHOW DATABASES;
   ```

### 问题 4：端口被占用

**解决方案：**

**修改后端端口（application.yml）：**
```yaml
server:
  port: 8888  # 改为其他端口
```

**修改前端端口（vue.config.js）：**
```javascript
module.exports = {
  devServer: {
    port: 8889
  }
}
```

### 问题 5：前端页面空白

**解决方案：**

1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签的错误信息
3. 查看 Network 标签，检查 API 请求是否成功
4. 确保：
   - 后端服务已启动
   - `frontend/src/main.js` 中的 baseURL 正确
   - 浏览器没有缓存问题（按 Ctrl+F5 强制刷新）

---

## 📚 项目结构说明

```
MusicMaster/
├── backend/              # 后端项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/     # Java 源代码
│   │   │   └── resources/
│   │   │       ├── application.yml  # 配置文件
│   │   │       └── sql/    # 数据库脚本
│   │   └── test/      # 测试代码
│   └── pom.xml         # Maven 配置
├── frontend/            # 前端项目
│   ├── public/         # 静态资源
│   ├── src/
│   │   ├── assets/     # 资源文件
│   │   ├── components/ # 公共组件
│   │   ├── views/      # 页面组件
│   │   ├── router/     # 路由配置
│   │   └── main.js     # 入口文件
│   └── package.json    # npm 配置
├── docs/               # 文档
│   ├── 应用背景.md
│   ├── 技术文档.md
│   └── 部署教程.md
└── README.md           # 项目说明
```

---

## 🎯 核心功能测试清单

启动系统后，按以下顺序测试：

- [ ] **用户登录**
  - 用户名：admin
  - 密码：admin123

- [ ] **用户管理**
  - 查看用户列表
  - 添加新用户
  - 编辑用户信息
  - 删除用户

- [ ] **歌手管理**
  - 查看歌手列表
  - 添加歌手
  - 上传歌手图片
  - 删除歌手

- [ ] **歌曲管理**
  - 查看歌曲列表
  - 添加歌曲
  - 上传音乐文件
  - 播放测试

- [ ] **歌单管理**
  - 创建新歌单
  - 添加歌曲到歌单
  - 编辑歌单信息

- [ ] **评论功能**
  - 发表评论
  - 点赞评论
  - 删除评论

- [ ] **收藏功能**
  - 收藏歌单
  - 查看我的收藏
  - 取消收藏

- [ ] **数据统计**
  - 查看用户统计
  - 查看歌曲统计
  - 查看活跃度分析

---

## 💡 下一步操作

1. **修改默认密码**
   - 登录后立即修改 admin 用户的密码

2. **添加测试数据**
   - 添加一些歌手信息
   - 上传一些歌曲
   - 创建示例歌单

3. **个性化配置**
   - 修改系统标题
   - 更换默认图片
   - 调整系统参数

4. **部署到服务器**
   - 参考 `docs/部署教程.md` 中的生产环境部署部分

---

## 🆘 获取帮助

如果遇到问题：

1. 查看 `docs/部署教程.md` 中的详细说明
2. 检查日志文件：
   - 后端日志：控制台输出
   - 前端日志：浏览器开发者工具

3. 常见问题参考上方的"常见问题快速解决"部分

---

## 📝 版本信息

- **项目名称**：MusicMaster 音乐管理系统
- **版本**：v1.0.0
- **最后更新**：2024-03-11
- **维护者**：LWJSTONE

---

祝您使用愉快！🎉
