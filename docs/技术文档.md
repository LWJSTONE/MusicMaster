# MusicMaster 音乐管理系统 - 技术文档

## 系统架构

### 1. 整体架构

MusicMaster 采用前后端分离的架构设计，通过 RESTful API 进行数据交互。

```
┌─────────────────┐         ┌─────────────────┐
│                 │         │                 │
│   Vue.js 前端    │◄────────►│  Spring Boot    │
│   (ElementUI)    │  API    │   后端服务       │
│                 │         │                 │
└─────────────────┘         └─────────────────┘
                                      │
                                      ▼
                              ┌─────────────────┐
                              │    MySQL 数据库   │
                              └─────────────────┘
```

### 2. 技术栈

#### 后端技术

- **Spring Boot 2.7.18**
  - 核心框架，提供快速开发能力
  - 内嵌 Tomcat 服务器
  - 自动配置和依赖注入

- **MyBatis Plus 3.5.4**
  - 持久层框架
  - 提供通用 CRUD 操作
  - 分页插件支持

- **Druid 1.2.20**
  - 数据库连接池
  - SQL 监控功能
  - 性能优化

- **BCrypt**
  - 密码加密
  - 安全性保障

#### 前端技术

- **Vue.js 2.6.14**
  - 渐进式 JavaScript 框架
  - 组件化开发
  - 双向数据绑定

- **ElementUI 2.15.14**
  - Vue 2.0 组件库
  - 丰富的 UI 组件
  - 响应式设计

- **Vue Router 3.5.3**
  - 官方路由管理器
  - 路由守卫
  - 嵌套路由

- **Axios 1.6.0**
  - HTTP 客户端
  - Promise API
  - 请求/响应拦截

#### 数据库技术

- **MySQL 8.0+**
  - 关系型数据库
  - 事务支持
  - 完善的索引机制

## 数据库设计

### 1. 数据库表结构

#### 用户表 (user)
```sql
CREATE TABLE `user` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `password` varchar(255) NOT NULL COMMENT '密码',
  `nickname` varchar(50) DEFAULT NULL COMMENT '昵称',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `avatar` varchar(255) DEFAULT NULL COMMENT '头像URL',
  `role` tinyint(4) DEFAULT '0' COMMENT '用户角色：0-普通用户，1-管理员',
  `status` tinyint(4) DEFAULT '1' COMMENT '状态：0-禁用，1-启用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
);
```

#### 歌手表 (singer)
```sql
CREATE TABLE `singer` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL COMMENT '歌手姓名',
  `sex` tinyint(4) DEFAULT '0' COMMENT '性别：0-女，1-男，2-组合',
  `birth` date DEFAULT NULL COMMENT '出生日期',
  `location` varchar(100) DEFAULT NULL COMMENT '地区',
  `introduction` varchar(500) DEFAULT NULL COMMENT '简介',
  `pic` varchar(255) DEFAULT NULL COMMENT '歌手头像URL',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`)
);
```

#### 歌曲表 (song)
```sql
CREATE TABLE `song` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `name` varchar(100) NOT NULL COMMENT '歌曲名称',
  `singer_id` bigint(20) DEFAULT NULL COMMENT '歌手ID',
  `singer_name` varchar(100) DEFAULT NULL COMMENT '歌手名称',
  `album` varchar(100) DEFAULT NULL COMMENT '专辑名称',
  `style` varchar(50) DEFAULT NULL COMMENT '歌曲风格',
  `language` varchar(50) DEFAULT NULL COMMENT '歌曲语言',
  `url` varchar(255) DEFAULT NULL COMMENT '歌曲URL',
  `pic` varchar(255) DEFAULT NULL COMMENT '歌曲封面图片URL',
  `duration` int(11) DEFAULT NULL COMMENT '歌曲时长（秒）',
  `play_count` int(11) DEFAULT '0' COMMENT '播放量',
  `comment_count` int(11) DEFAULT '0' COMMENT '评论数',
  `collect_count` int(11) DEFAULT '0' COMMENT '收藏数',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_singer_id` (`singer_id`)
);
```

#### 歌单表 (song_list)
```sql
CREATE TABLE `song_list` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL COMMENT '歌单标题',
  `pic` varchar(255) DEFAULT NULL COMMENT '歌单封面图片URL',
  `introduction` varchar(500) DEFAULT NULL COMMENT '歌单简介',
  `style` varchar(50) DEFAULT NULL COMMENT '风格',
  `creator_id` bigint(20) DEFAULT NULL COMMENT '创建者ID',
  `creator_name` varchar(50) DEFAULT NULL COMMENT '创建者用户名',
  `collect_count` int(11) DEFAULT '0' COMMENT '收藏数',
  `play_count` int(11) DEFAULT '0' COMMENT '播放量',
  `song_count` int(11) DEFAULT '0' COMMENT '歌曲数量',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_creator_id` (`creator_id`)
);
```

#### 评论表 (comment)
```sql
CREATE TABLE `comment` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) DEFAULT NULL COMMENT '用户ID',
  `username` varchar(50) DEFAULT NULL COMMENT '用户名',
  `avatar` varchar(255) DEFAULT NULL COMMENT '用户头像',
  `song_id` bigint(20) DEFAULT NULL COMMENT '歌曲ID',
  `song_list_id` bigint(20) DEFAULT NULL COMMENT '歌单ID',
  `type` tinyint(4) DEFAULT '0' COMMENT '评论类型：0-歌曲评论，1-歌单评论',
  `content` text COMMENT '评论内容',
  `up` int(11) DEFAULT '0' COMMENT '点赞数',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_song_id` (`song_id`),
  KEY `idx_song_list_id` (`song_list_id`)
);
```

#### 收藏表 (collect)
```sql
CREATE TABLE `collect` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) NOT NULL COMMENT '用户ID',
  `song_list_id` bigint(20) NOT NULL COMMENT '歌单ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `deleted` tinyint(4) DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_song_list` (`user_id`, `song_list_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_song_list_id` (`song_list_id`)
);
```

### 2. 数据库索引设计

- 主键索引：所有表的 id 字段
- 唯一索引：user.username, collect(user_id, song_list_id)
- 普通索引：song.singer_id, song_list.creator_id, comment.user_id, comment.song_id, comment.song_list_id

## API 接口文档

### 1. 用户管理接口

#### 用户登录
```http
POST /api/user/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

#### 用户注册
```http
POST /api/user/register
Content-Type: application/json

{
  "username": "newuser",
  "password": "password123",
  "nickname": "新用户",
  "email": "user@example.com"
}
```

#### 分页查询用户
```http
GET /api/user/page?current=1&size=10&username=admin
```

### 2. 歌手管理接口

#### 添加歌手
```http
POST /api/singer
Content-Type: application/json

{
  "name": "周杰伦",
  "sex": 1,
  "birth": "1979-01-18",
  "location": "台湾",
  "introduction": "华语流行乐坛天王"
}
```

#### 分页查询歌手
```http
GET /api/singer/page?current=1&size=10&name=周杰伦
```

### 3. 歌曲管理接口

#### 添加歌曲
```http
POST /api/song
Content-Type: application/json

{
  "name": "晴天",
  "singerId": 1,
  "singerName": "周杰伦",
  "album": "叶惠美",
  "style": "流行",
  "language": "国语",
  "url": "https://music.example.com/song1.mp3",
  "pic": "https://example.com/pic1.jpg",
  "duration": 269
}
```

#### 分页查询歌曲
```http
GET /api/song/page?current=1&size=10&name=晴天&singerId=1
```

### 4. 歌单管理接口

#### 添加歌单
```http
POST /api/song-list
Content-Type: application/json

{
  "title": "华语流行经典",
  "introduction": "精选华语流行音乐",
  "style": "流行",
  "creatorId": 1,
  "creatorName": "admin"
}
```

#### 分页查询歌单
```http
GET /api/song-list/page?current=1&size=10&title=流行
```

### 5. 评论管理接口

#### 添加评论
```http
POST /api/comment
Content-Type: application/json

{
  "userId": 1,
  "username": "admin",
  "avatar": "https://example.com/avatar.jpg",
  "songId": 1,
  "type": 0,
  "content": "这首歌很好听！"
}
```

#### 分页查询评论
```http
GET /api/comment/page?current=1&size=10&songId=1&type=0
```

### 6. 收藏管理接口

#### 添加收藏
```http
POST /api/collect
Content-Type: application/json

{
  "userId": 1,
  "songListId": 1
}
```

#### 取消收藏
```http
DELETE /api/collect?userId=1&songListId=1
```

## 项目分层架构

### 1. 后端分层

```
Controller 层 (控制器)
    ↓
Service 层 (业务逻辑)
    ↓
Mapper 层 (数据访问)
    ↓
Database (数据库)
```

#### Controller 层
- 负责接收 HTTP 请求
- 参数校验和格式转换
- 调用 Service 层处理业务
- 返回统一响应格式

#### Service 层
- 实现业务逻辑
- 事务管理
- 数据校验和处理
- 调用 Mapper 层操作数据库

#### Mapper 层
- 数据库操作接口
- SQL 语句执行
- 结果集映射

### 2. 前端分层

```
View 层 (页面组件)
    ↓
Component 层 (公共组件)
    ↓
API 层 (接口封装)
    ↓
Axios (HTTP 请求)
```

#### View 层
- 页面展示和交互
- 用户事件处理
- 数据绑定和渲染

#### Component 层
- 可复用的 UI 组件
- 封装通用功能
- 组件通信机制

#### API 层
- 统一的接口封装
- 请求拦截器
- 响应数据处理

## 安全设计

### 1. 密码加密
- 使用 BCrypt 加密算法
- 随机盐值增强安全性
- 不可逆加密存储

### 2. 用户认证
- 基于用户名密码登录
- Token 存储在 localStorage
- 路由守卫验证权限

### 3. 权限控制
- 用户角色区分（普通用户/管理员）
- 前端路由权限控制
- 后端接口权限验证

### 4. 数据保护
- SQL 注入防护（MyBatis Plus）
- XSS 攻击防护
- CSRF 攻击防护

## 性能优化

### 1. 数据库优化
- 合理的索引设计
- 查询语句优化
- 连接池配置（Druid）

### 2. 缓存策略
- 静态资源缓存
- 接口数据缓存
- 浏览器缓存控制

### 3. 代码优化
- 分页查询避免全表扫描
- 批量操作减少数据库交互
- 前端组件懒加载

## 异常处理

### 1. 统一异常处理
- 自定义异常类
- 全局异常捕获
- 友好的错误提示

### 2. 日志记录
- 操作日志记录
- 错误日志收集
- 日志分级管理

## 扩展性设计

### 1. 模块化设计
- 业务模块独立
- 接口标准化
- 配置外部化

### 2. 可插拔架构
- 组件化开发
- 依赖注入
- 配置文件管理

### 3. 微服务准备
- RESTful API
- 无状态设计
- 服务治理基础

## 测试策略

### 1. 单元测试
- Service 层业务逻辑测试
- Mapper 层数据操作测试

### 2. 集成测试
- API 接口测试
- 前后端联调测试

### 3. 性能测试
- 并发压力测试
- 响应时间测试
- 数据库性能测试