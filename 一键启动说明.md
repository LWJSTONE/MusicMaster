# MusicMaster 一键启动说明

## 快速启动指南

### 首次使用步骤

1. **准备运行环境**
   
   将以下环境解压到 `runtime` 目录：
   - JDK 8 -> `runtime/jdk/`
   - Node.js 18 -> `runtime/nodejs/`
   - Maven 3.9+ -> `runtime/maven/`
   - MySQL 8.0（可选）-> `runtime/mysql/`

   **下载地址：**
   - JDK 8: https://adoptium.net/temurin/releases/?version=8
   - Node.js 18: https://nodejs.org/
   - Maven 3.9: https://maven.apache.org/download.cgi
   - MySQL 8.0: https://dev.mysql.com/downloads/mysql/

2. **双击运行 `start.bat`**

3. **按照菜单提示操作：**
   - 选择 `[2]` 初始化数据库
   - 选择 `[1]` 一键启动系统

### 环境目录结构要求

```
runtime/
├── jdk/                    # JDK 8 便携版
│   └── bin/
│       └── java.exe        # 必须存在
├── nodejs/                 # Node.js 18 便携版
│   └── node.exe            # 必须存在
├── maven/                  # Maven 便携版
│   └── bin/
│       └── mvn.cmd         # 必须存在
└── mysql/                  # MySQL 8.0 便携版（可选）
    └── bin/
        └── mysqld.exe
```

### 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 (64位) |
| 内存 | 至少 4GB 可用 |
| 磁盘 | 至少 2GB 可用空间 |

### 端口使用

| 服务 | 端口 |
|------|------|
| 前端服务 | 8081 |
| 后端服务 | 8080 |
| MySQL | 3306 |

### 默认登录信息

- **管理员账号**: admin
- **管理员密码**: 123456
- **登录后请及时修改密码**

## 目录结构

```
MusicMaster/
├── start.bat              # 主启动脚本（双击运行）
├── stop.bat               # 停止服务脚本
├── init-database.bat      # 数据库初始化脚本
├── runtime/               # 运行环境目录
│   ├── jdk/               # JDK 便携版
│   ├── nodejs/            # Node.js 便携版
│   ├── maven/             # Maven 便携版
│   └── mysql/             # MySQL 便携版（可选）
├── backend/               # 后端项目
│   └── src/main/resources/
│       └── application.yml # 后端配置文件
└── frontend/              # 前端项目
    └── vue.config.js      # 前端配置文件
```

## 环境隔离说明

本脚本使用内置运行环境，**不会修改系统环境变量**：
- 所有环境变量仅在脚本运行期间有效
- 关闭脚本窗口后，环境变量自动释放
- 不会影响系统中已安装的其他软件

## 配置修改

### 修改数据库连接

编辑文件: `backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/musicmaster?...
    username: root
    password: root  # 修改为你的MySQL密码
```

### 修改前端端口

编辑文件: `frontend/vue.config.js`

```javascript
module.exports = defineConfig({
  devServer: {
    port: 8081  // 修改为你想要的端口
  }
})
```

### 修改后端端口

编辑文件: `backend/src/main/resources/application.yml`

```yaml
server:
  port: 8080  # 修改为你想要的端口
```

## 常见问题

### Q: 提示环境未安装?

**解决方案:**

确保以下文件存在：
- `runtime/jdk/bin/java.exe`
- `runtime/nodejs/node.exe`
- `runtime/maven/bin/mvn.cmd`

### Q: MySQL 连接失败?

**解决方案:**
1. 确保 MySQL 服务已启动
2. 检查用户名密码是否正确
3. 检查端口 3306 是否被占用

### Q: 端口被占用?

**解决方案:**
```cmd
# 查看端口占用
netstat -ano | findstr :8080
netstat -ano | findstr :8081

# 结束占用进程
taskkill /F /PID <进程ID>
```

### Q: 前端页面空白?

**解决方案:**
1. 按F12打开浏览器开发者工具
2. 检查Console是否有错误
3. 检查Network面板API请求状态
4. 确保后端服务已正常启动

### Q: 中文显示乱码?

**解决方案:**
本脚本使用ANSI编码兼容，不支持UTF-8特殊字符。
如果仍有乱码，请将CMD窗口字体设置为"新宋体"或"Consolas"。

## 技术支持

如遇到问题，请检查：
1. 各服务窗口的错误日志
2. 浏览器开发者工具的控制台

---
MusicMaster v1.0.0 | 作者: LWJSTONE
