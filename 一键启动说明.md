# MusicMaster 一键启动说明

## 快速启动指南

### 首次使用步骤

1. **双击运行 `start.bat`**
2. **按照菜单提示操作：**
   - 选择 `[2]` 安装运行环境（JDK、Node.js、Maven）
   - 选择 `[3]` 安装 MySQL 便携版（或使用系统已安装的MySQL）
   - 选择 `[4]` 初始化数据库
   - 选择 `[1]` 一键启动系统

### 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 (64位) |
| 内存 | 至少 4GB 可用 |
| 磁盘 | 至少 2GB 可用空间（用于环境安装）|
| 网络 | 需要网络连接（下载环境组件）|

### 端口使用

| 服务 | 端口 |
|------|------|
| 前端服务 | 8081 |
| 后端服务 | 8080 |
| MySQL | 3306 |

### 默认登录信息

- **管理员账号**: admin
- **管理员密码**: admin123
- **登录后请及时修改密码**

## 目录结构

```
MusicMaster/
├── start.bat              # 主启动脚本（双击运行）
├── stop.bat               # 停止服务脚本
├── init-database.bat      # 数据库初始化脚本
├── runtime/               # 运行环境目录
│   ├── install-env.bat    # 环境安装脚本
│   ├── install-mysql.bat  # MySQL安装脚本
│   ├── stop-mysql.bat     # MySQL停止脚本
│   ├── jdk/               # JDK 便携版
│   ├── nodejs/            # Node.js 便携版
│   ├── maven/             # Maven 便携版
│   └── mysql/             # MySQL 便携版
├── backend/               # 后端项目
│   └── src/main/resources/
│       └── application.yml # 后端配置文件
└── frontend/              # 前端项目
    └── vue.config.js      # 前端配置文件
```

## 配置修改

### 修改数据库连接

编辑文件: `backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/musicmaster?...
    username: root
    password: root  # 修改为你的MySQL密码
```

### 修改前端端口

编辑文件: `frontend/vue.config.js`

```javascript
module.exports = defineConfig({
  devServer: {
    port: 8081  // 修改为你想要的端口
  }
})
```

### 修改后端端口

编辑文件: `backend/src/main/resources/application.yml`

```yaml
server:
  port: 8080  # 修改为你想要的端口
```

## 常见问题

### Q: 环境下载失败?

**解决方案:**
1. 检查网络连接
2. 手动下载以下组件并解压到 `runtime` 目录：
   - JDK 8: https://adoptium.net/temurin/releases/?version=8
   - Node.js 18: https://nodejs.org/
   - Maven 3.9: https://maven.apache.org/download.cgi

### Q: MySQL 连接失败?

**解决方案:**
1. 确保 MySQL 服务已启动
2. 检查用户名密码是否正确
3. 检查端口 3306 是否被占用

### Q: 端口被占用?

**解决方案:**
```cmd
# 查看端口占用
netstat -ano | findstr :8080
netstat -ano | findstr :8081

# 结束占用进程
taskkill /F /PID <进程ID>
```

### Q: 前端页面空白?

**解决方案:**
1. 按F12打开浏览器开发者工具
2. 检查Console是否有错误
3. 检查Network面板API请求状态
4. 确保后端服务已正常启动

## 技术支持

如遇到问题，请检查：
1. 各服务窗口的错误日志
2. 浏览器开发者工具的控制台

---
MusicMaster v1.0.0 | 作者: LWJSTONE
